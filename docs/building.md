# Building

The canonical build documentation now lives in the repository root:

```text
README.md
```

Keep platform-specific build instructions there so Windows, Linux, macOS,
Android, runtime deployment, troubleshooting, and project policy do not drift
apart.

Short Windows reminder:

```bat
build_xash.bat
build_game.bat
copyfiles.bat
```
