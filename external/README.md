# External Sources

This directory is reserved for pinned upstream source trees.

- `xash3d-fwgs` is the engine.
- `hlsdk-portable` is the portable Half-Life SDK used as the starting SDK layer.

Both are Git submodules. Use the root scripts instead of editing or building directly in these folders when possible.
